import React from 'react';
import { View } from 'react-native';

const GameScreen = () => {

    return (
        <View>
            This is the game page
        </View>
    );
};

export default GameScreen;