import React from 'react';
import { View, FlatList } from 'react-native';

const GameScreen = () => {

    return (
        <View>
            <FlatList></FlatList>
        </View>
    );
};

export default GameScreen;